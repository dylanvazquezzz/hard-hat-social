---
phase: 03-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/NavBar.tsx
autonomous: false
requirements:
  - UX-03

must_haves:
  truths:
    - "On a 375px-wide viewport the desktop nav links are hidden and a hamburger button is visible in their place"
    - "Tapping the hamburger button opens a dropdown panel containing Directory, Explore, Jobs, Apply (if logged out), and auth state links"
    - "The hamburger button tap target is at least 44px in height and width"
    - "Clicking a link in the mobile dropdown closes the menu and navigates to the correct page"
    - "The desktop navigation (links + account dropdown) remains unchanged at md: breakpoint and above"
  artifacts:
    - path: "components/NavBar.tsx"
      provides: "Hamburger button with menuOpen state, mobile dropdown panel, responsive visibility classes"
      contains: "menuOpen"
  key_links:
    - from: "hamburger button (md:hidden)"
      to: "mobile dropdown panel"
      via: "menuOpen state toggle onClick"
      pattern: "setMenuOpen"
    - from: "menuRef useRef"
      to: "click-outside handler"
      via: "document mousedown event listener"
      pattern: "menuRef.current"
---

<objective>
Add a hamburger menu to NavBar.tsx for mobile viewports (below 768px) so that navigation is accessible at 375px without tiny icons or hidden links.

Purpose: Tradespeople use phones — an unusable mobile nav is a conversion blocker before the founding cohort even tries the platform.
Output: components/NavBar.tsx modified with hamburger button, menuOpen state, menuRef, and mobile dropdown panel.
</objective>

<execution_context>
@/Users/dylanvazquez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dylanvazquez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@components/NavBar.tsx
</context>

<interfaces>
<!-- Full current NavBar.tsx state — executor modifies this file. -->

From components/NavBar.tsx (current state):
```tsx
'use client'

import { useEffect, useRef, useState } from 'react'
import { useRouter } from 'next/navigation'
import { supabase } from '@/lib/supabase'
import type { Session } from '@supabase/supabase-js'

export default function NavBar() {
  const router = useRouter()
  const [session, setSession] = useState<Session | null>(null)
  const [username, setUsername] = useState<string | null>(null)
  const [dropdownOpen, setDropdownOpen] = useState(false)
  const dropdownRef = useRef<HTMLDivElement>(null)

  // ... auth effects, fetchUsername, handleSignOut ...

  // Click-outside useEffect (lines 42-50):
  useEffect(() => {
    function handleClickOutside(e: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) {
        setDropdownOpen(false)
      }
    }
    document.addEventListener('mousedown', handleClickOutside)
    return () => document.removeEventListener('mousedown', handleClickOutside)
  }, [])

  // JSX — the nav content area (lines 67-139):
  // <div className="flex items-center gap-6">
  //   <a href="/contractors">Directory</a>
  //   <a href="/explore">Explore</a>
  //   <a href="/jobs">Jobs</a>
  //   {!session && <a href="/apply">Apply</a>}
  //
  //   {session ? (
  //     <div className="relative" ref={dropdownRef}>
  //       <button onClick={() => setDropdownOpen(o => !o)} ...>
  //         @username / Account + chevron
  //       </button>
  //       {dropdownOpen && (
  //         <div className="absolute right-0 mt-2 w-44 ...">
  //           <a href="/profile">Profile</a>
  //           <button onClick={handleSignOut}>Sign Out</button>
  //         </div>
  //       )}
  //     </div>
  //   ) : (
  //     <a href="/auth" className="...amber bg...">Sign In</a>
  //   )}
  // </div>
}
```

Key: The outer nav wrapper is `<div className="flex h-16 items-center justify-between">`. The right side is `<div className="flex items-center gap-6">` containing all nav links plus auth.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add hamburger state, ref, and mobile dropdown to NavBar.tsx</name>
  <files>components/NavBar.tsx</files>
  <action>
    Modify components/NavBar.tsx to add hamburger mobile navigation. The changes are additive — existing desktop nav and account dropdown are preserved exactly.

    **Step 1 — Add state and ref** (alongside existing declarations):
    ```tsx
    const [menuOpen, setMenuOpen] = useState(false)
    const menuRef = useRef<HTMLDivElement>(null)
    ```

    **Step 2 — Extend the click-outside useEffect** to also close the hamburger menu:
    Replace the existing handleClickOutside function body with:
    ```tsx
    function handleClickOutside(e: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) {
        setDropdownOpen(false)
      }
      if (menuRef.current && !menuRef.current.contains(e.target as Node)) {
        setMenuOpen(false)
      }
    }
    ```

    **Step 3 — Restructure the nav content div** (the `<div className="flex items-center gap-6">` and its contents). Replace this div with two sibling elements:

    First element — desktop nav (hidden on mobile):
    ```tsx
    <div className="hidden md:flex items-center gap-6">
      <a href="/contractors" className="text-sm text-slate-400 hover:text-slate-100 transition-colors">Directory</a>
      <a href="/explore" className="text-sm text-slate-400 hover:text-slate-100 transition-colors">Explore</a>
      <a href="/jobs" className="text-sm text-slate-400 hover:text-slate-100 transition-colors">Jobs</a>
      {!session && (
        <a href="/apply" className="text-sm text-slate-400 hover:text-slate-100 transition-colors">Apply</a>
      )}
      {session ? (
        <div className="relative" ref={dropdownRef}>
          <button
            onClick={() => setDropdownOpen((o) => !o)}
            className="flex items-center gap-1 rounded-md bg-slate-800 px-4 py-2 text-sm font-semibold text-slate-100 hover:bg-slate-700 transition-colors"
          >
            {username ? `@${username}` : 'Account'}
            <svg
              className={`h-4 w-4 transition-transform ${dropdownOpen ? 'rotate-180' : ''}`}
              fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}
            >
              <path strokeLinecap="round" strokeLinejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          {dropdownOpen && (
            <div className="absolute right-0 mt-2 w-44 rounded-md border border-slate-700 bg-slate-800 shadow-lg">
              <a href="/profile" onClick={() => setDropdownOpen(false)}
                className="block px-4 py-2 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors rounded-t-md">
                Profile
              </a>
              <button onClick={handleSignOut}
                className="block w-full text-left px-4 py-2 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors rounded-b-md">
                Sign Out
              </button>
            </div>
          )}
        </div>
      ) : (
        <a href="/auth" className="rounded-md bg-amber-500 px-4 py-2 text-sm font-semibold text-slate-950 hover:bg-amber-400 transition-colors">
          Sign In
        </a>
      )}
    </div>
    ```

    Second element — hamburger (visible on mobile only):
    ```tsx
    <div className="relative md:hidden" ref={menuRef}>
      <button
        onClick={() => setMenuOpen((o) => !o)}
        className="flex items-center justify-center rounded-md p-3 text-slate-400 hover:text-slate-100 hover:bg-slate-800 transition-colors"
        aria-label="Open navigation menu"
        aria-expanded={menuOpen}
      >
        {menuOpen ? (
          <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        ) : (
          <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        )}
      </button>

      {menuOpen && (
        <div className="absolute right-0 mt-2 w-56 rounded-md border border-slate-700 bg-slate-800 shadow-lg">
          <a href="/contractors" onClick={() => setMenuOpen(false)}
            className="block px-4 py-3 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors rounded-t-md">
            Directory
          </a>
          <a href="/explore" onClick={() => setMenuOpen(false)}
            className="block px-4 py-3 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors">
            Explore
          </a>
          <a href="/jobs" onClick={() => setMenuOpen(false)}
            className="block px-4 py-3 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors">
            Jobs
          </a>
          {!session && (
            <a href="/apply" onClick={() => setMenuOpen(false)}
              className="block px-4 py-3 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors">
              Apply
            </a>
          )}
          {session ? (
            <>
              <a href="/profile" onClick={() => setMenuOpen(false)}
                className="block px-4 py-3 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors">
                {username ? `@${username}` : 'Profile'}
              </a>
              <button
                onClick={() => { setMenuOpen(false); handleSignOut() }}
                className="block w-full text-left px-4 py-3 text-sm text-slate-300 hover:bg-slate-700 hover:text-slate-100 transition-colors rounded-b-md"
              >
                Sign Out
              </button>
            </>
          ) : (
            <a href="/auth" onClick={() => setMenuOpen(false)}
              className="block px-4 py-3 text-sm font-semibold text-amber-400 hover:bg-slate-700 transition-colors rounded-b-md">
              Sign In
            </a>
          )}
        </div>
      )}
    </div>
    ```

    The outer nav right-side becomes a `<div className="flex items-center gap-2">` wrapping both the desktop nav div and the hamburger div.

    **Constraints:**
    - Hamburger button uses `p-3` padding on a `h-5 w-5` icon — total tap target = 20px + 12px + 12px = 44px (meets WCAG 2.5.5)
    - Mobile dropdown uses `right-0` positioning — at 375px viewport and `w-56` (224px) the dropdown fits without clipping
    - Desktop nav: `hidden md:flex` — hidden below 768px, visible at 768px+
    - Hamburger: `md:hidden` on the wrapper — hidden at 768px+, visible below
    - Use inline SVG paths (no Heroicons package — per CLAUDE.md: ask before adding npm packages)
    - The `dropdownRef` stays on the account dropdown div only (desktop). The `menuRef` covers the hamburger button + dropdown div.
    - Both dropdowns close on click-outside via the extended handleClickOutside
  </action>
  <verify>
    <automated>cd /Users/dylanvazquez/Desktop/contractors-connect && npm run lint && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    components/NavBar.tsx compiles cleanly. The file contains: menuOpen state, menuRef, extended handleClickOutside closing both dropdowns, desktop nav div with hidden md:flex, hamburger button div with md:hidden, mobile dropdown panel with all nav links and auth state, p-3 padding on hamburger button. npm run lint and build pass with no errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify hamburger nav at 375px viewport</name>
  <what-built>Hamburger mobile navigation in NavBar.tsx — hamburger button at mobile breakpoint, dropdown panel with all links, desktop nav unchanged above md:.</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project directory
    2. Open http://localhost:3000 in a browser
    3. Open DevTools (F12) and enable responsive mode at 375px width
    4. Confirm: desktop nav links (Directory, Explore, Jobs, Apply) are NOT visible
    5. Confirm: a hamburger icon (three horizontal bars) is visible in the top-right, inside the navbar
    6. Tap/click the hamburger — confirm a dropdown opens with: Directory, Explore, Jobs, Apply (if logged out) or @username + Sign Out (if logged in), and Sign In (amber, if logged out)
    7. Confirm the hamburger button area is at least 44px tall (inspect element — should show p-3 padding + 20px icon = 44px)
    8. Click any link in the dropdown — confirm the menu closes and navigates correctly
    9. Click outside the dropdown — confirm it closes
    10. Switch to desktop width (1200px) — confirm the hamburger is gone and original nav links appear
  </how-to-verify>
  <resume-signal>Type "approved" when the hamburger nav works correctly at 375px, or describe any issues</resume-signal>
  <action>Pause for human verification. No automated action — executor waits for user to confirm hamburger navigation works at 375px viewport per the how-to-verify steps above.</action>
  <verify>
    <automated>MISSING — visual browser interaction required; no automated test framework in project</automated>
  </verify>
  <done>User types "approved" confirming hamburger nav works at 375px with correct dropdown links and tap targets.</done>
</task>

</tasks>

<verification>
- NavBar.tsx compiles with no TypeScript errors (`npm run build`)
- No new npm packages added
- Hamburger button: `md:hidden`, `p-3` padding, aria-label, aria-expanded, inline SVG hamburger/X icons
- Desktop nav: `hidden md:flex` — same links and account dropdown as before
- Mobile dropdown: w-56, right-0, all nav links with onClick={() => setMenuOpen(false)}, auth-conditional Sign In / @username + Sign Out
- menuRef covers the hamburger + dropdown wrapper div
- handleClickOutside closes both dropdownOpen and menuOpen
- Manual browser check at 375px viewport confirms UX-03 success criteria
</verification>

<success_criteria>
- UX-03: A user on a 375px-wide screen can open and use the navigation menu by tapping elements that are at least 44px in height
- Desktop navigation unchanged — all existing nav links and account dropdown work at md: breakpoint and above
- npm run lint && npm run build exit 0
- Human verification approved at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-polish/03-02-SUMMARY.md`
</output>
