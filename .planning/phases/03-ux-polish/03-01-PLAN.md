---
phase: 03-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/contractors/loading.tsx
  - app/contractors/page.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Loading the /contractors directory shows card-shaped skeleton placeholders during data fetch instead of a blank screen"
    - "Applying filters that return no results shows a message and a Reset filters button"
    - "Clicking Reset filters returns the directory to its unfiltered state (/contractors)"
  artifacts:
    - path: "app/contractors/loading.tsx"
      provides: "9-card skeleton grid matching ContractorCard layout with animate-pulse"
      exports: ["default function ContractorsLoading"]
    - path: "app/contractors/page.tsx"
      provides: "Upgraded empty state with SVG icon, message, and Reset filters anchor"
      contains: "Reset filters"
  key_links:
    - from: "app/contractors/loading.tsx"
      to: "app/contractors/page.tsx"
      via: "Next.js Suspense convention — loading.tsx auto-wraps page.tsx"
      pattern: "export default function.*Loading"
    - from: "app/contractors/page.tsx"
      to: "/contractors"
      via: "a href anchor on Reset filters button"
      pattern: "href=\"/contractors\""
---

<objective>
Add loading skeleton UI and an upgraded empty state to the /contractors directory so the page communicates clearly during data fetch and when filters return no results.

Purpose: The directory is the core product — it must feel responsive and handle edge states gracefully before real users arrive.
Output: app/contractors/loading.tsx (new), app/contractors/page.tsx (empty state block upgraded).
</objective>

<execution_context>
@/Users/dylanvazquez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dylanvazquez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/contractors/page.tsx
@components/ContractorCard.tsx
</context>

<interfaces>
<!-- Key structures the executor needs. Extracted from codebase. -->

From app/contractors/page.tsx (lines 53-104) — outer layout structure to mirror in loading.tsx:
```tsx
// Outer wrapper
<div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8">

  {/* Page header */}
  <div className="mb-8">
    <h1 className="text-3xl font-bold text-slate-100">Contractor Directory</h1>
    <p className="mt-2 text-slate-400">...</p>
  </div>

  {/* Two-column layout */}
  <div className="flex flex-col gap-8 lg:flex-row">
    <aside className="w-full shrink-0 lg:w-56"> {/* filter sidebar */} </aside>
    <div className="flex-1 space-y-10">

      {/* Empty state (lines 76-81) — to be upgraded */}
      {contractors.length === 0 ? (
        <div className="flex flex-col items-center justify-center rounded-lg border border-slate-800 bg-slate-900 py-24 text-center">
          <p className="text-slate-400">No contractors found.</p>
          <p className="mt-1 text-sm text-slate-500">Try adjusting your filters.</p>
        </div>
      ) : (
        <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"> {/* card grid */} </div>
      )}

    </div>
  </div>
</div>
```

From components/ContractorCard.tsx — card structure to replicate in skeleton:
```tsx
// Card outer
<div className="rounded-lg border border-slate-800 bg-slate-900 p-5">
  <div className="flex items-start gap-4">

    {/* Avatar: h-12 w-12 rounded-full */}
    <div className="h-12 w-12 shrink-0 rounded-full bg-slate-800" />

    <div className="min-w-0 flex-1">
      {/* Name row + Verified badge */}
      <div className="flex items-center justify-between gap-2">
        <h3 className="truncate font-semibold text-slate-100">...</h3>
        <span className="shrink-0 rounded-full bg-emerald-900/50 px-2 py-0.5 text-xs text-emerald-400">Verified</span>
      </div>
      {/* Trade: text-sm text-amber-400 */}
      <p className="text-sm font-medium text-amber-400">...</p>
      {/* Location · yrs: mt-1 text-xs text-slate-500 */}
      <p className="mt-1 text-xs text-slate-500">...</p>
      {/* Specialty tags: mt-2 flex flex-wrap gap-1, rounded bg-slate-800 px-2 py-0.5 text-xs */}
    </div>
  </div>
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create app/contractors/loading.tsx with 9-card skeleton grid</name>
  <files>app/contractors/loading.tsx</files>
  <action>
    Create a new file at app/contractors/loading.tsx. This file is automatically used by Next.js as the Suspense fallback for the /contractors route while page.tsx fetches data. No changes to page.tsx are needed for this task.

    The loading UI must mirror the exact outer layout structure of page.tsx to prevent layout shift on transition:
    - Same `mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8` wrapper
    - Page header skeleton: a wide bar for the h1 (`h-9 w-56`) and a narrower bar for the subtext (`h-4 w-40`), both `bg-slate-800 animate-pulse rounded`
    - Same `flex flex-col gap-8 lg:flex-row` two-column layout
    - Aside placeholder: empty `<div className="w-full shrink-0 lg:w-56" />` — reserves sidebar space but shows no skeleton (per user decision: sidebar excluded from skeletons)
    - Flex-1 content area with the 9-card skeleton grid: `grid gap-4 sm:grid-cols-2 xl:grid-cols-3`

    Each of the 9 skeleton cards must replicate the ContractorCard structure:
    - Card outer: `rounded-lg border border-slate-800 bg-slate-900 p-5 animate-pulse` (animate-pulse on wrapper animates all children together)
    - `flex items-start gap-4` inner layout
    - Avatar placeholder: `h-12 w-12 shrink-0 rounded-full bg-slate-800`
    - Name + badge row: `flex items-center justify-between gap-2` with `h-4 w-32 rounded bg-slate-800` (name) and `h-4 w-14 rounded-full bg-slate-800` (badge)
    - Trade line: `h-3 w-20 rounded bg-slate-800 mt-1`
    - Location line: `h-3 w-36 rounded bg-slate-800 mt-1`
    - Two specialty tag placeholders: `mt-2 flex gap-1` with `h-4 w-16 rounded bg-slate-800` and `h-4 w-12 rounded bg-slate-800`

    Use `Array.from({ length: 9 }).map((_, i) => <SkeletonCard key={i} />)` to render 9 cards. Define SkeletonCard as a local function component within the file.

    Do NOT apply animate-pulse to the loading.tsx outer wrapper or the aside — only to each SkeletonCard. The header skeleton elements get their own `animate-pulse` inline.

    Use `animate-pulse` (Tailwind built-in) — do NOT add custom CSS keyframes.
  </action>
  <verify>
    <automated>cd /Users/dylanvazquez/Desktop/contractors-connect && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    app/contractors/loading.tsx exists and exports a default function. npm run build passes with no TypeScript errors. The file replicates the page.tsx outer layout structure and renders 9 SkeletonCard components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade empty state in app/contractors/page.tsx</name>
  <files>app/contractors/page.tsx</files>
  <action>
    Replace the existing empty state block (lines 76-81) in app/contractors/page.tsx with the upgraded version below. The rest of the file is untouched.

    Existing empty state to replace:
    ```tsx
    <div className="flex flex-col items-center justify-center rounded-lg border border-slate-800 bg-slate-900 py-24 text-center">
      <p className="text-slate-400">No contractors found.</p>
      <p className="mt-1 text-sm text-slate-500">Try adjusting your filters.</p>
    </div>
    ```

    New empty state (drop-in replacement):
    ```tsx
    <div className="flex flex-col items-center justify-center rounded-lg border border-slate-800 bg-slate-900 py-24 text-center">
      <svg
        className="mb-4 h-12 w-12 text-slate-700"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        strokeWidth={1.5}
      >
        <path
          strokeLinecap="round"
          strokeLinejoin="round"
          d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z"
        />
      </svg>
      <p className="font-medium text-slate-300">No contractors match your filters</p>
      <p className="mt-1 text-sm text-slate-500">Try broadening your search or clearing all filters.</p>
      <a
        href="/contractors"
        className="mt-6 rounded-md bg-slate-800 px-4 py-2 text-sm font-medium text-slate-200 hover:bg-slate-700 transition-colors"
      >
        Reset filters
      </a>
    </div>
    ```

    Use `<a href="/contractors">` (not router.push) — page.tsx is a server component and must remain one. The anchor tag produces identical navigation behavior and requires no client component.

    Do NOT add 'use client' to page.tsx. Do NOT change any other part of the file.
  </action>
  <verify>
    <automated>cd /Users/dylanvazquez/Desktop/contractors-connect && npm run lint && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    page.tsx empty state block contains the SVG magnifying glass icon, the message "No contractors match your filters", the sub-message, and an anchor tag with href="/contractors" labeled "Reset filters". npm run lint and build pass cleanly.
  </done>
</task>

</tasks>

<verification>
- `app/contractors/loading.tsx` exists and exports a default function
- `loading.tsx` mirrors the outer layout structure of `page.tsx` (same max-w-7xl, flex layout, aside placeholder width)
- 9 SkeletonCard components rendered in the same grid (sm:grid-cols-2 xl:grid-cols-3) as real ContractorCard components
- SkeletonCard replicates avatar, name row, trade, location, and specialty tag placeholders using bg-slate-800 divs
- `animate-pulse` applied to each SkeletonCard wrapper (not the outer loading wrapper)
- `page.tsx` empty state contains SVG icon, updated message, and `<a href="/contractors">Reset filters</a>`
- `page.tsx` remains a server component (no 'use client' directive added)
- `npm run lint && npm run build` passes with no errors
</verification>

<success_criteria>
- UX-01: Next.js automatically shows the skeleton grid during /contractors data fetch (force-dynamic triggers a fresh fetch on every navigation; loading.tsx is shown for that duration)
- UX-02: When contractors.length === 0, the SVG icon, "No contractors match your filters" message, and "Reset filters" button are visible; clicking Reset filters navigates to /contractors with no search params
- npm run build exits 0 with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-polish/03-01-SUMMARY.md`
</output>
